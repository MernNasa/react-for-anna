what is hooks in react..?
it's a just javascript reusable functions.

always hooks prefix with use word
and also this hooks follows camelCase

ex: useState | useRef | useEffect 

hooks are two types in react
1. Built-in hooks (react)
2.custom hooks


Rules for to define hooks
1. Hooks are always need to declare top level of the function

1.useState hook
it is used to store the data and update the data dynamically

syntax:

const [value,function]=useState(initialvalue)
