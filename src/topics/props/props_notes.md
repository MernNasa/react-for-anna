props---> properties in react

props is nothing but sending the data from parent component to child component.

props----> object
key:value
<!-- 
<Child propname=propvalue>


const Child=(props:object)=>{
    props.propname
} -->


types of props 

default props

unidirectional
parent ---> child
immutable in nature


primitive and non-primitive datatypes 

Spread Props
prop name and prop value 
if we need to send props to child without prop name then we need the spread operator
optional props
Alias props


